% layout 'default';
% title 'One article';
<h2><%= $category %></h2>
<p>
<%= $mtime %><br />
<%= $msg %><br />

</p>
<p>
&nbsp;&nbsp;&nbsp;
<em>
% foreach my $tag (keys %{$tags}) {
	<a href="/tag/<%= $tags->{$tag}->{name} %>" title="<%= $tags->{$tag}->{tag_id} %>">
		<%= $tags->{$tag}->{name} %>
	</a>
	&nbsp;
% }
</em>
</p>

<p><a href="/">Main page</a></p>
