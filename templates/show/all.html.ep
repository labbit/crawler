% layout 'default';
% title 'list all articles';
<h2>List articles</h2>
<p>
<ul>
% foreach my $text (keys %{$articles}) {
	<li>
		<b><%= $articles->{$text}->{cat} %></b>, 
		<%= $articles->{$text}->{mtime} %>, 
		<a href="/<%= $articles->{$text}->{aid} %>">
			<%= $articles->{$text}->{text} %>
		</a>

	</li>
% }
</ul>


</p>
